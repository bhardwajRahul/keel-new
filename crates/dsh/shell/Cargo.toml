[package]
name = "dsh-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Shell capability: ctx.subprocess managed process groups, the ctx.shell request/spec seam with the local bash executor, and the model-facing bash tool (port of the @deepseek-ai/dsh-subprocess* and dsh-shell/bash/tool-bash packages)."

[dependencies]
anyhow = { workspace = true }
dsh-agent = { workspace = true }
dsh-cordis = { workspace = true }
dsh-llm = { workspace = true }
dsh-timeout = { workspace = true }
dsh-tools = { workspace = true }
futures = { workspace = true }
libc = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
dsh-session = { workspace = true }
tempfile = { workspace = true }
