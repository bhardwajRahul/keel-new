[package]
name = "dsh-launch-environment"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Immutable launch-time environment snapshot with per-value provenance (port of @deepseek-ai/dsh-launch-environment)."

[dependencies]
